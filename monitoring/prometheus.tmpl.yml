global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "vedh-api"
    metrics_path: /prometheus
    static_configs:
      - targets:
          - "${PROMETHEUS_TARGET}"
    authorization:
      type: Bearer
      credentials: "${PROMETHEUS_BEARER_TOKEN}"
