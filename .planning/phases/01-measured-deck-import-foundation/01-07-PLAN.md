---
phase: 01-measured-deck-import-foundation
plan: 07
type: execute
wave: 6
depends_on: ["01-06"]
files_modified:
  - docs/research/deck-provider-feasibility.md
  - server/deck_providers.go
  - server/deck_providers_test.go
  - server/deck_import.go
  - server/deck_import_test.go
  - server/graphql.go
  - server/testdata/deck_providers/
  - .planning/phases/01-measured-deck-import-foundation/COVERAGE.md
autonomous: false
requirements: [REQ-ACT-003]

user_setup:
  - service: "public deck provider (selected at the decision checkpoint in this plan)"
    why: "D-16 permits the adapter to use app-level credentials that vEDH holds. This entry applies ONLY if the checkpoint selects a provider that requires one; if the checkpoint records a no-go, or selects a provider with an unauthenticated public read path, nothing here is needed and the entry is closed as not-applicable in the plan summary."
    env_vars:
      - name: DECK_PROVIDER_API_KEY
        source: "the selected provider's developer console — recorded in docs/research/deck-provider-feasibility.md at the checkpoint"
      - name: DECK_PROVIDER_ENABLED
        source: "set to true in the Dokku app config only after the allowlist is set; defaults to false"
      - name: DECK_PROVIDER_ALLOWED_HOSTS
        source: "the selected provider's exact hostname, recorded in the decision record"
    dashboard_config:
      - task: "Set the three variables above on the API app"
        location: "Dokku app config for the server deployment"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A written decision record exists that evaluates both candidate providers against the same six criteria and either names a first supported provider or documents a reasoned no-go."
    - "The decision record states, for each candidate, whether an unauthenticated public read path exists, how a private deck responds, what rate-limit signals appear, whether the response shape is stable, what the terms-of-service and operational risk are, and whether a fixture can be captured and maintained."
    - "The record's recommendation rests on response-shape stability as the only hard gate condition, with terms-of-service permission, rate limits, and credential requirements recorded as context for the human rather than as independent gate failures."
    - "No locked network control was relaxed, widened, made optional, or made configurable-off anywhere in the repository between the start and the end of this plan — the deny-prefix count, the timeout values, the size cap, the exact-equality host match, and the redirect bound are all unchanged from plan 01-06."
    - "In the provider branch: a checked-in fixture body parses through the canonical parser into a normalized deck, and a fixture whose shape has changed fails the contract test rather than silently producing a different deck."
    - "In the provider branch: a private or otherwise unavailable deck is reported as unsupported in product language, and no code path requests, stores, or transmits a user's third-party account credentials."
    - "In the no-go branch: the paste-only path is complete and the kill switch remains present and defaulting to off, so the Phase 5 release runbook has a concrete switch to reference either way."
    - "A coverage matrix or a reasoned no-integration declaration exists at the phase directory's COVERAGE.md, matching whichever branch the checkpoint selected."
    - "No test added by this plan makes a network connection outside the loopback interface."
  prohibitions:
    - statement: "No SSRF, timeout, redirect, or response-size control may be relaxed, widened, or made configurable-off in order to make a candidate provider pass the feasibility gate. If a provider cannot be reached within the locked controls, the correct outcome is a no-go, not a weaker control."
    - statement: "The provider path MUST NOT request, store, or transmit a user's third-party account credentials. A deck that is private to its owner is reported as unsupported; app-level credentials that vEDH itself holds are a different thing and are permitted."
  artifacts:
    - path: "docs/research/deck-provider-feasibility.md"
      provides: "the timeboxed neutral comparison and the recommendation the checkpoint decides on"
      min_lines: 60
    - path: ".planning/phases/01-measured-deck-import-foundation/COVERAGE.md"
      provides: "the external-API coverage matrix, or a reasoned declaration that this phase integrates no external API"
  key_links:
    - from: "server/deck_import.go"
      to: "server/deck_providers.go"
      via: "the enabled provider branch routes a deck URL through the secure client built in plan 01-06"
      pattern: "providerEnabled"
    - from: "server/deck_providers.go"
      to: "pkg/deckimport/scanner.go"
      via: "a provider response is normalized through the canonical parser, never through a second grammar"
      pattern: "deckimport\\.Parse"
---

<objective>
Run the one-day timeboxed feasibility comparison the ticket calls for, put its evidence in front of a
human at a decision checkpoint, and then execute whichever branch the human picks — a first provider
adapter behind the already-built secure client, or a documented no-go with paste-only activation
shipping.

Purpose: OPEN-1 is genuinely open. D-13 requires the spike to start neutral and D-14 requires the
checkpoint to come before any adapter code, so this plan deliberately does not pre-write either
branch's implementation in detail. Both outcomes satisfy this phase's fifth success criterion, and a
no-go must not block the Phase 5 release gate — it changes what "provider fallback" means in that
gate, not whether the gate can close.

Output: a decision record, a resolved checkpoint, the selected branch executed, and the external-API
coverage decision written down.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INGEST-CONFLICTS.md
@.planning/intel/constraints.md
@.planning/phases/01-measured-deck-import-foundation/01-CONTEXT.md
@.planning/phases/01-measured-deck-import-foundation/01-RESEARCH.md
@.planning/phases/01-measured-deck-import-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeboxed neutral feasibility comparison and decision record</name>
  <precondition>Outbound HTTPS from the executor's environment is available — if it is not, write that limitation into the record and halt at the checkpoint rather than substituting recalled or inferred provider behaviour for observed evidence — AND, for this task's `go test ./server` regression gate, a PostgreSQL 14 instance is reachable at DATABASE_URL (or the compose default postgres://edhgo:edhgo@localhost:5432/edhgo) with either `../All Printings.json` present or ALL_PRINTINGS_JSON_PATH naming a readable MTGJSON AllPrintings-shaped file, because server/main_test.go TestMain hard-exits without both.</precondition>
  <files>docs/research/deck-provider-feasibility.md, server/testdata/deck_providers/</files>
  <read_first>
    - .planning/phases/01-measured-deck-import-foundation/01-CONTEXT.md decisions D-13, D-14, D-15 and D-16 — the neutrality requirement, the checkpoint placement, the single hard gate condition, and the app-level-credential allowance
    - .planning/intel/constraints.md sections "Deck provider SSRF and network controls" and "Scope guardrails"
    - .planning/REQUIREMENTS.md REQ-ACT-003 including its exit note
    - .planning/INGEST-CONFLICTS.md item INFO-1
    - .planning/phases/01-measured-deck-import-foundation/01-RESEARCH.md Open Question 1 (line 1120) — the six things the spike must measure for each candidate
    - server/deck_providers.go as built in plan 01-06 — the controls a candidate must be reachable within, unmodified
    - docs/plans/2026-07-23-deck-to-game-activation-tickets.md, the ACT-003 ticket — its acceptance criteria and verification steps
  </read_first>
  <action>
Write `docs/research/deck-provider-feasibility.md`. Evaluate both candidates — Archidekt and Moxfield
— against the same criteria, in the same depth, in the same document. Start neutral and let the
evidence decide: no candidate is favoured going in, and the record must read as a comparison rather
than as a justification for a conclusion already reached.

For each candidate, record all six of the following, each with the observed evidence that supports it
and the date it was observed:

1. **Request shape and public read path.** Is there an unauthenticated way to read a public deck? What
   is the exact request, and what does a successful response look like? Capture one real response body
   for a public deck and save it under `server/testdata/deck_providers/` with a filename naming the
   provider and the capture date. Redact nothing structural, but remove any account identifier that is
   not needed to exercise the parse.
2. **Private-deck behaviour.** What does the provider return for a deck that is private to its owner?
   This determines the "reported as unsupported without requesting third-party credentials" behaviour
   the constraint requires, so record the exact status and body shape rather than a summary.
3. **Rate-limit signals.** What headers or status codes indicate throttling, and at what observed
   request rate?
4. **Response-shape stability.** Is the response a documented, versioned contract, or an internal
   shape that can change without notice? What evidence exists either way — a published schema, a
   versioned path segment, a changelog? Per D-15 this is the **only hard gate condition**: everything
   else in this list is context for the human, not an independent failure.
5. **Terms of service and operational risk.** What do the terms permit for automated reads? Record it
   plainly, including if it is ambiguous. This is recorded for the human approving at the checkpoint
   and does not by itself fail the gate.
6. **Fixture maintainability.** Can a representative response be captured and kept as a checked-in
   fixture, and what would make it go stale? The standing constraint forbids any test depending on a
   live provider, so a candidate whose response cannot be fixtured is a candidate whose adapter cannot
   be tested.

Also record, per candidate, whether the provider is reachable **within the locked controls exactly as
they exist in `server/deck_providers.go` today** — secure scheme only, exact-equality host allowlist,
three-second connect, eight-second total, one-mebibyte cap, bounded redirects that stay on the
allowlist. If a candidate is only reachable by relaxing one of those, that is a no-go for that
candidate, not a reason to change the control. Say so explicitly in the record.

Record whether the candidate needs an app-level credential that vEDH would hold. That is permitted by
D-16 and is distinct from asking a user for their own credentials, which remains forbidden. If a
credential is needed, note the deployment consequence: a server-side secret enters the Dokku deploy
path, and the release-gate tests stay credential-free because they run against fixtures.

Close the document with a recommendation section that states one of three positions — Archidekt,
Moxfield, or no-go — with the reasoning tied to the hard gate condition, and a short list of what the
human should weigh at the checkpoint. Do not implement anything on the basis of the recommendation;
the checkpoint is where the branch is chosen.

Keep the effort inside the ticket's one-day box. If a candidate cannot be characterised within that
box, record what is known, record what remains unknown, and let the checkpoint decide with that
uncertainty visible rather than papered over.
  </action>
  <verify>
    <automated>test -s docs/research/deck-provider-feasibility.md</automated>
    <automated>grep -qi 'archidekt' docs/research/deck-provider-feasibility.md &amp;&amp; grep -qi 'moxfield' docs/research/deck-provider-feasibility.md</automated>
    <automated>grep -ci 'response.shape\|response shape' docs/research/deck-provider-feasibility.md | awk '$1 >= 1 { exit 0 } { exit 1 }'</automated>
    <automated>git diff --exit-code HEAD -- server/deck_providers.go || { echo "the secure client must be unchanged by the spike"; exit 1; }</automated>
    <automated>go test ./server -run 'TestSafeControl|TestSafeClient' -race</automated>
  </verify>
  <acceptance_criteria>
    - `docs/research/deck-provider-feasibility.md` covers both candidates and all six criteria for each, with an observation date on every evidence item.
    - The record states, per candidate, whether the provider is reachable within the locked controls exactly as they exist in `server/deck_providers.go`, and states explicitly that a candidate reachable only by relaxing a control is a no-go for that candidate.
    - The recommendation section names exactly one of Archidekt, Moxfield, or no-go, and ties the reasoning to response-shape stability as the hard gate condition.
    - Terms of service, rate limits, and credential requirements each appear as recorded context rather than as a stated gate failure.
    - At least one real response body per reachable candidate is checked in under `server/testdata/deck_providers/` with the provider and capture date in the filename; if a candidate was unreachable, the record says so instead.
    - `server/deck_providers.go` is byte-unchanged by this task (`git diff --exit-code HEAD` over that path exits 0) and the plan 01-06 secure-client suite still passes.
    - Where evidence could not be gathered inside the timebox, the record names the specific unknown rather than filling it with an inference.
  </acceptance_criteria>
  <done>A neutral, evidence-backed comparison of both candidates exists with a stated recommendation, and no network control was touched to produce it.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Choose the first supported deck provider, or record a no-go</name>
  <decision>Which public deck provider becomes vEDH's first supported URL source — Archidekt, Moxfield, or neither (paste-only activation ships)?</decision>
  <context>
This is OPEN-1, open since ingest, owned by ACT-003 and deliberately unanswered during planning per
D-13. D-14 places this checkpoint before any adapter code exists, so nothing has been built toward
either outcome and neither branch is cheaper to pick.

What the decision costs to undo: selecting a provider means an adapter, a checked-in fixture that has
to be kept current, an exact hostname in the production allowlist, and — if that provider needs an
app-level credential — a server-side secret in the Dokku deploy path. Reversing it means removing
that secret plumbing and may invalidate the selection itself. Choosing no-go costs nothing to reverse:
the secure client and the kill switch are already built and default to off, so a later phase can add
an adapter without revisiting anything decided here.

What this decision does **not** change: paste-based activation ships either way, and the Phase 5
release gate can close either way. INFO-1 is explicit that a no-go must not block it — a no-go changes
what "provider fallback" means in that gate, not whether the gate can close.

Read `docs/research/deck-provider-feasibility.md` before deciding. Per D-15 the only hard gate
condition is response-shape stability; terms-of-service permission, rate limits, and credential
requirements are recorded there for your judgement but none of them fails the gate on its own. Per the
scope guardrails, no SSRF or reliability control may be weakened to make a candidate pass — if the
record says a candidate is only reachable outside the locked controls, that candidate is not
available here.
  </context>
  <options>
    <option id="archidekt">
      <name>Ship an Archidekt adapter first</name>
      <pros>As recorded in the "Archidekt" section of docs/research/deck-provider-feasibility.md — public read path, private-deck behaviour, rate-limit signals, response-shape stability evidence, and fixture maintainability. Weigh the stability finding most heavily.</pros>
      <cons>As recorded in the same section, plus any app-level credential requirement and its Dokku secret consequence, and the ongoing cost of keeping the fixture current.</cons>
    </option>
    <option id="moxfield">
      <name>Ship a Moxfield adapter first</name>
      <pros>As recorded in the "Moxfield" section of docs/research/deck-provider-feasibility.md, across the same six criteria. Weigh the stability finding most heavily.</pros>
      <cons>As recorded in the same section, plus any app-level credential requirement and its Dokku secret consequence, and the ongoing cost of keeping the fixture current.</cons>
    </option>
    <option id="no-go">
      <name>Record a no-go — paste-only activation ships</name>
      <pros>Zero ongoing maintenance surface, no third-party dependency in the activation path, no secret in the deploy path, and no fixture that can go stale. The kill switch and secure client already exist and default to off, so a later phase can add an adapter with no rework. This outcome fully satisfies this phase's fifth success criterion.</pros>
      <cons>Players who keep decks in an online builder must copy and paste rather than pasting a link, which is a small amount of extra friction on the activation path this milestone is measuring. Phase 5's provider-failure journey is evaluated against a build where the URL path is absent rather than broken.</cons>
    </option>
  </options>
  <resume-signal>Select: archidekt, moxfield, or no-go</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the selected branch and record the coverage decision</name>
  <reversibility rating="one-way">In the provider branch this task commits vEDH to a specific external service: an adapter written against one provider's response shape, that provider's hostname in the production allowlist, a checked-in fixture that must be maintained, and — under D-16 — potentially a server-side secret in the deploy path. Undoing it removes secret-management plumbing from deploy and invalidates the selection. The preceding checkpoint is the gate on that door. In the no-go branch nothing irreversible is committed.</reversibility>
  <precondition>The decision checkpoint in task 2 has been resolved and its outcome is recorded in docs/research/deck-provider-feasibility.md — do not begin this task from the record's recommendation alone — AND a PostgreSQL 14 instance is reachable at DATABASE_URL (or the compose default postgres://edhgo:edhgo@localhost:5432/edhgo) with either `../All Printings.json` present or ALL_PRINTINGS_JSON_PATH naming a readable MTGJSON AllPrintings-shaped file, because every test in this task runs in `package server`, whose TestMain hard-exits without both.</precondition>
  <files>server/deck_providers.go, server/deck_providers_test.go, server/deck_import.go, server/deck_import_test.go, server/graphql.go, .planning/phases/01-measured-deck-import-foundation/COVERAGE.md</files>
  <read_first>
    - docs/research/deck-provider-feasibility.md including the recorded checkpoint outcome
    - server/deck_providers.go and server/deck_providers_test.go as built in plan 01-06 — the secure client, the kill switch, and the hermetic test conventions this task extends
    - server/deck_import.go as completed in plan 01-06 — the enabled-provider branch this task finishes
    - pkg/deckimport/scanner.go as completed in plan 01-02 — the canonical parser a provider response must be normalized through
    - server/testdata/deck_providers/ — the response bodies captured in task 1
    - .planning/intel/constraints.md sections "Provider feature flag and kill switch" and "Testing and release-gate coverage"
    - $HOME/.claude/gsd-core/references/... is not needed here; the coverage-matrix format is reproduced in the action below
  </read_first>
  <action>
Execute exactly one branch, determined by the checkpoint outcome. Do not partially execute both.

**If the checkpoint selected a provider.**
Define an adapter interface in `server/deck_providers.go` keyed by an allowlisted hostname, so a
second provider in a later milestone is an added implementation rather than a rewrite. Implement one
adapter for the selected provider: it fetches through the secure client built in plan 01-06 — never
its own client, never its own transport — and converts the response body into decklist text that is
then handed to the canonical parser. Normalizing through that parser rather than building a second
grammar is a locked constraint: a provider path that parsed differently from the paste path would
mean two parsers under divergent rules, which is precisely what the one-canonical-parser rule exists
to prevent.

Map every provider failure onto a normalized, product-language error with a paste-text fallback
message: an unreachable host, a non-success status, a private or missing deck, an unparseable body,
and a body over the cap. A private deck is reported as unsupported and no path requests, stores, or
transmits a user's third-party credentials. The raw provider body goes to the request-scoped log and
never to the client.

If the selected provider requires an app-level credential, add it as an envconfig field on `Conf`
named `DECK_PROVIDER_API_KEY` defaulting to empty, treat an empty value as "provider disabled" in
`providerEnabled()`, and record the Dokku configuration step in the plan summary as
deployment-affecting. Never log the value and never include it in an error returned to a client.

Add `TestProvider_FixtureContract` in `server/deck_providers_test.go`: parse each checked-in fixture
body through the adapter and the canonical parser and assert the resulting normalized deck against a
checked-in expectation, so a refreshed fixture whose shape has moved fails the test rather than
silently producing a different deck. Add `TestProvider_PrivateDeckIsUnsupported` and
`TestProvider_ErrorsAreNormalized` covering every failure mapping above. Every one of these runs
against a loopback test server or a fixture; none makes an outbound connection.

Write `.planning/phases/01-measured-deck-import-foundation/COVERAGE.md` as a markdown table with the
columns `capability`, `decision`, `reason`. Enumerate the selected provider's public capability
surface — at minimum: read a public deck, read a private deck, list a user's decks, read deck
metadata such as name and format, read a deck's commander designation, search decks, and any other
endpoint the feasibility record observed. Start every capability at INTEGRATE and subtract
deliberately: each OPT-OUT carries a one-line reason. Reading a private deck is an OPT-OUT whose
reason is that the project will not ask users for third-party credentials. Anything scoped to a later
milestone is an OPT-OUT naming that scope. An opt-out with no reason is an undecided hole, which is
the exact failure this matrix exists to close.

**If the checkpoint recorded a no-go.**
Add a closing section to `docs/research/deck-provider-feasibility.md` stating the no-go, the date, the
reasoning tied to the hard gate condition, and the explicit note that paste-based activation ships and
that this outcome satisfies this phase's fifth success criterion. Add a second note recording the
reinterpretation Phase 5 needs: its provider-failure journey is evaluated against a build where the
URL path is absent rather than broken, and that reinterpretation is settled in
`/gsd-discuss-phase 5`, not here.

Leave the secure client and the kill switch exactly as plan 01-06 built them. Do not delete them: the
flag defaulting to off is what gives the Phase 5 release runbook a concrete switch to reference, and
the secure client is what makes a later provider addition a small change rather than a security
review. Confirm by test that the deck-URL branch remains a normalized paste-fallback error and that
the pasted path is unaffected — the plan 01-06 kill-switch tests already assert this; add
`TestProvider_NoGoPathIsStable` asserting the deck-URL branch returns the paste-fallback error with
the flag in either state, since no adapter exists to enable.

Write `.planning/phases/01-measured-deck-import-foundation/COVERAGE.md` containing exactly the
reasoned declaration line, with the reason naming the no-go and pointing at the decision record — for
example, a line of the form `No external API integration: <reason>`. That declaration is accepted in
place of a matrix, and the reason is required exactly as an opt-out reason would be.

**In both branches.** Re-verify that no locked control moved: the deny-prefix count, the connect and
total budgets, the size cap, the exact-equality host match, and the redirect bound are all identical
to plan 01-06. Run the full plan 01-06 secure-client suite unchanged.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./server</automated>
    <automated>go test ./server -run 'TestSafeControl|TestSafeClient|TestProvider_' -race -v</automated>
    <automated>go test ./pkg/... -race</automated>
    <automated>test -s .planning/phases/01-measured-deck-import-foundation/COVERAGE.md</automated>
    <automated>grep -q 'MustParsePrefix' server/deck_providers.go &amp;&amp; grep -c 'MustParsePrefix' server/deck_providers.go | awk '$1 >= 18 { exit 0 } { exit 1 }'</automated>
    <automated>! grep -q 'ProxyFromEnvironment' server/deck_providers.go &amp;&amp; ! grep -q 'IsGlobalUnicast' server/deck_providers.go</automated>
    <automated>grep -qE 'No external API integration: .+' .planning/phases/01-measured-deck-import-foundation/COVERAGE.md || grep -qE '^\| *[a-z_ -]+ *\| *(INTEGRATE|OPT-OUT) *\|' .planning/phases/01-measured-deck-import-foundation/COVERAGE.md</automated>
    <automated>git grep -n 'time.Second' -- server/deck_providers.go | grep -Eq '(3 \* time.Second|8 \* time.Second)'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one branch was executed, and which one matches the checkpoint outcome recorded in `docs/research/deck-provider-feasibility.md`.
    - `.planning/phases/01-measured-deck-import-foundation/COVERAGE.md` exists and either contains a `capability | decision | reason` table in which every row's decision is INTEGRATE or OPT-OUT and every OPT-OUT carries a non-empty reason, or contains a single reasoned no-integration declaration naming the no-go and pointing at the decision record.
    - Provider branch only: `TestProvider_FixtureContract`, `TestProvider_PrivateDeckIsUnsupported` and `TestProvider_ErrorsAreNormalized` all pass, and every one of them runs against a loopback server or a checked-in fixture with no outbound connection.
    - Provider branch only: the adapter fetches through the client built in plan 01-06 and constructs no client, transport, or dialer of its own — verified by reading the diff and by the absence of any additional transport construction in `server/deck_providers.go`.
    - Provider branch only: the provider response is converted to text and handed to the canonical parser; no second grammar exists anywhere in `server/`.
    - Provider branch only: a private deck is reported as unsupported and no code path reads, stores, or transmits a user's third-party credentials.
    - Provider branch only: if an app-level credential is required, it is an envconfig field defaulting to empty, an empty value disables the provider, and the value appears in no log line and no client-facing error.
    - No-go branch only: the decision record carries a dated closing section stating the no-go, the reasoning, that paste-based activation ships, and the Phase 5 reinterpretation note; `TestProvider_NoGoPathIsStable` passes.
    - Both branches: the deny-prefix count is still at least 18, the connect and total budgets are still 3 and 8 seconds, the transport proxy field is still nil, the global-unicast predicate is still unused, and the full plan 01-06 secure-client suite passes unchanged.
    - Both branches: `go test ./pkg/... -race` is green.
  </acceptance_criteria>
  <done>The checkpoint's chosen branch is fully executed — a working, fixture-tested adapter behind the existing secure client, or a dated no-go with paste-only activation intact — and the external-API coverage decision is written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected provider → server process | In the provider branch, a third party's response body enters the parse path |
| deployment environment → provider credential | In the provider branch under D-16, a server-side secret enters the Dokku deploy path |
| feasibility spike → repository | Captured response bodies become checked-in fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29 | Tampering | the feasibility gate itself | high | mitigate | The locked controls are unchanged by this plan and re-verified at the end of every task; the record must state, per candidate, that reachability was assessed against the controls exactly as they exist, and a candidate reachable only outside them is recorded as a no-go for that candidate |
| T-01-30 | Information Disclosure | app-level provider credential | high | mitigate | Envconfig field defaulting to empty, empty treated as disabled, never logged, never present in a client-facing error; the release-gate tests stay credential-free because they run against fixtures |
| T-01-31 | Information Disclosure | user third-party credentials | high | mitigate | No code path requests, stores, or transmits them; a private deck is reported as unsupported in product language, proven by `TestProvider_PrivateDeckIsUnsupported` |
| T-01-11 | Information Disclosure | provider error text | medium | mitigate | Every provider failure maps to a normalized product-language error with a paste-text fallback; the raw body goes to the request-scoped log |
| T-01-32 | Tampering | checked-in provider fixtures | medium | mitigate | `TestProvider_FixtureContract` asserts each fixture against a checked-in expected normalized deck, so a refreshed fixture whose shape moved fails loudly instead of silently changing what a deck parses to |
| T-01-33 | Denial of Service | third-party availability on the activation path | medium | mitigate | The kill switch defaults to off and can be flipped independently of guest creation, so the release can drop to paste-only without a deploy of new code; every provider failure already falls back to paste |
| T-01-06 | Information Disclosure | outbound fetch | critical | mitigate | Inherited unchanged from plan 01-06 and re-verified here: the adapter constructs no client of its own and can only reach the network through the controlled one |
</threat_model>

## Artifacts this phase produces

See the complete phase-level artifact index in `01-01-PLAN.md` under the same heading. Every symbol
listed there is created by Phase 1 and does not exist at baseline `main@ef2732a`; drift verification
must treat all of them as new. This plan creates `docs/research/deck-provider-feasibility.md`,
`.planning/phases/01-measured-deck-import-foundation/COVERAGE.md`, the fixture bodies under
`server/testdata/deck_providers/`, and — in the provider branch only — the adapter interface and one
implementation inside `server/deck_providers.go` plus the conditional `DECK_PROVIDER_API_KEY`
environment variable.

**Why COVERAGE.md arrives here rather than at plan time.** The `api-coverage` gate will detect this
phase as an external-API integration once the plan bodies exist. A coverage matrix cannot honestly be
written before the checkpoint: D-13 and D-14 keep the provider deliberately unchosen until task 2, so
any matrix authored at plan time would enumerate the capability surface of a service that had not
been selected. Fabricated rows for an unselected provider would be worse than a late matrix, and the
alternative declaration — that this phase integrates no external API — would be false in the provider
branch. `COVERAGE.md` is therefore a deliverable of task 3, written against whichever branch the
human selected, and the `verify:pre` seal gate finds it in place because this plan is the phase's
last wave.

<verification>
- `docs/research/deck-provider-feasibility.md` covers both candidates against all six criteria and states one recommendation.
- The decision checkpoint is resolved and its outcome is recorded in the decision record.
- Exactly one branch is executed and its test suite passes; `go test ./pkg/... -race` remains green.
- `COVERAGE.md` exists in the phase directory in the form matching the selected branch.
- Every locked network control is byte-identical to plan 01-06 and the plan 01-06 secure-client suite passes unchanged.
- No test in the repository reaches a live deck provider.
</verification>

<success_criteria>
- A written decision record names the first supported provider and its adapter fetches only allowlisted secure hosts with redirects, rebinding, private addresses, oversized responses and timeouts all failing closed behind a kill switch — **or** it documents a reasoned no-go and paste-only activation proceeds. Either outcome satisfies this phase.
- No SSRF or reliability control was weakened to make a candidate pass.
- No user is ever asked for a third-party credential, and a private deck is reported as unsupported.
- The external-API coverage decision is recorded, matching the branch taken.
</success_criteria>

<output>
Create `.planning/phases/01-measured-deck-import-foundation/01-07-SUMMARY.md` when done.
</output>